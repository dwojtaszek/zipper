namespace Zipper.LoadFiles;

/// <summary>
/// Identifies the output mode for DAT and OPT load file writers.
/// </summary>
internal enum WriterMode
{
    Standard,
    LoadfileOnly,
    ProductionSet,
}
