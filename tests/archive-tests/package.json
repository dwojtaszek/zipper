{
  "name": "zipper-archive-test-verifier",
  "version": "1.0.0",
  "private": true,
  "description": "Pinned Ajv CLI for offline independent verification of Archive Test Fixtures (ticket #932). Install once with `npm ci`; verification itself never downloads.",
  "license": "MIT",
  "dependencies": {
    "ajv-cli": "5.0.0"
  }
}
